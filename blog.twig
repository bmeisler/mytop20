<ul>
    {% for post in posts %}
        <li><a href="{{ post.permalink }}">{{ post.title }}</a></li>
    {% else %}
        <li>Sorry about that, no posts matched your criteria</li>
    {% endfor %}
</ul>
